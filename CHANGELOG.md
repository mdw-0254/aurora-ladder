# 更新日志 / Changelog

本项目的所有重要变更都会记录在此文件中。

格式基于 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，
版本号遵循 [语义化版本 Semantic Versioning](https://semver.org/lang/zh-CN/)。

## [1.0.17] - 2026-08-28

### 修复 / Fixed

- **自动更新被安全软件拦截（覆盖 exe 操作）**：此前脚本尝试用 `Copy-Item` 覆盖旧 exe + 自启，公司环境安全软件拦截此高危操作。
  改为**不再覆盖旧 exe**，只做「等待旧进程退出 → 直接启动已下载的新版 exe」，让新文件独立运行，完全避开覆盖操作。
- 脚本首行立即写 `[auto] script started` 日志，进一步收敛后续排查范围。

## [1.0.15] - 2026-08-28

### 修复 / Fixed

- **自动更新进程无法派发（`spawn is not defined`）**：`main.js` 顶部仅导入了 `execFile`，漏掉了自动更新用到的 `spawn`，
  导致退出时派发 PowerShell 更新进程即抛错、不覆盖 exe 也不重启。已补上 `spawn` 导入。

### 测试 / Testing

- 本版本即修复版：发布 v1.0.15 后验证从旧版自动更新到 v1.0.15，关闭到托盘场景下点「立即关闭并更新」能自动覆盖 exe 并打开新版本

## [1.0.14] - 2026-08-28

### 修复 / Fixed

- **自动更新在「关闭到托盘」开启时不生效**：此前「立即关闭并更新」走 `window.close()`，会被"关闭到托盘"
  （`close` 事件 `preventDefault` + `hide()`）拦截而并未真正退出，导致自动覆盖不执行、重启后仍是旧版本。
  现改为 IPC `quitForUpdate`：先置 `quitting=true` 绕开托盘拦截 → 立即启动自动更新进程 → `app.quit()`，
  确保真正退出后覆盖 exe 并打开新版本
- 提示文案补充说明：开了"关闭到托盘"时请点更新按钮，勿只点窗口×

## [1.0.13] - 2026-08-28

### 测试 / Testing

- 用于验证「从 v1.0.12 自动升级到 v1.0.13」的更新闭环：版本检查走 Gitee、下载、退出自动覆盖 exe 并启动新版

## [1.0.12] - 2026-08-28

### 新增 / Added

- **自动更新（便携版）**：一键下载完成后，点击「立即关闭并更新」或直接关闭窗口，
  程序退出瞬间会启动一个分离式 PowerShell 进程 → 等本程序完全退出 → 用新安装包覆盖旧 exe → 自动启动新版本，
  全程无需手动替换/双击；中文路径自动适配（Base64 编码，无乱码）
- 更新浮层在便携版下新增「更新已就绪」引导与「立即关闭并更新」按钮；非便携版保留原手动指引
- 自动更新各步骤继续写入安装包目录下的 `update.log`，便于排查（杀软拦截 / 退出时序等）

### 变更 / Changed

- `update.log` 记录自动更新进程的派发与执行结果（[auto] spawned / replaced+started ok / failed）
- 版本号与 Gitee 发布 v1.0.12 对齐

## [1.0.11] - 2026-08-28

### 变更 / Changed

- **更新全程走 Gitee**：版本检查（`checkUpdate`）改为**优先查询 Gitee** Release（`mdw521/aurora-ladder`），
  取最新发行版的 exe 附件 → 直接构造 Gitee 下载直链；Gitee 不可达时才回退 GitHub 检查
- 下载源展开为「Gitee 直链(优先) → GitHub 加速镜像 → GitHub 直连(兜底)」，任一来源失败自动切换
- 此前「更新下载优先走 Gitee」的能力保留并进一步强化
- 版本号与 Gitee发布 v1.0.11 对齐

## [1.0.10] - 2026-08-28

### 变更 / Changed

- **更新下载源改为优先从 Gitee 拉取**：新增 Gitee 镜像源（`mdw521/aurora-ladder`），下载时先尝试 Gitee 直链（国内访问更稳定），
  失败自动回退到 GitHub 加速镜像与直连，不再因 GitHub 被限而下载失败
- **自更新改为「手动替换」稳妥方案**：放弃后台自动退出/覆盖/重启（公司安全软件普遍拦截该行为，历史 PowerShell/cmd 方案均因此失效）。
  现在一键更新只负责把 `目标版本安装包` 完整下载到程序同目录，完成后在界面弹出清晰指引：
  「先退出当前程序 → 双击运行新包（或用它覆盖旧程序文件）」并支持「打开所在文件夹 / 复制路径」
- 新增系统文件管理器中定位文件的 `app:showItemInFolder` IPC 与前端入口
- 版本号与 Gitee / GitHub Release `v1.0.10` 对齐

## [1.0.9] - 2026-08-28

### 修复 / Fixed

- 进一步定位「下载完成但始终不自动重启」：1.0.8 已保证主进程必然退出，但实测日志仍只到 `scheduling quit`，
  PowerShell 写下降 `apply: begin` 后再无任何后续（连等待心跳都没有），说明问题更可能在 PowerShell 子进程本身被系统/安全软件终止。
- 本次强化诊断，二选一定位：
  - 主进程注册 `will-quit` / `quit` / `exit` 钩子，并新增 2.8s 强制退出兜底 —— 确保旧进程必然退出，并在日志留下「主进程确已退出」的证据；
  - PowerShell 等待循环进入时立即写 `entering wait loop`，并每 5 秒打一次存活心跳 —— 若主进程已退出而 PS 无心跳，即可确认「PS 进程被安全软件终止」。
- 无论自动替换成败，下载的新包都会完整保留在 `release/`（`.update` 文件），可手动覆盖升级，不浪费流量
- 版本号与 GitHub Release `v1.0.9` 对齐

## [1.0.8] - 2026-08-28

### 修复 / Fixed

- 修复「一键更新」下载完成但**始终不自动重启**的根因：
  - 下载完成后主进程在 `app.quit()` 前会先 `await setSystemProxy(false)` 关闭系统代理。若该注册表/网络操作长期不返回，`app.quit()` 永远不会执行，
    PowerShell 替换脚本就会在「等待旧进程退出」的死循环里一直等待（最长 200 秒），移动/删除/启动新版全部不会发生 —— 即表现为"下载完成但不重启"。
  - 现在给关闭系统代理增加 **1000ms 超时**（`Promise.race`），确保 `app.quit()` 一定按时执行、主进程必然退出。
- PowerShell 替换脚本的等待循环增加**心跳日志**（每 50 次轮询记录一次），并在超时后强行进入替换流程，便于下次区分是「主进程未退出」还是「脚本被安全软件终止」
- 版本号与 GitHub Release `v1.0.8` 对齐

## [1.0.7] - 2026-08-28

### 修复 / Fixed

- 修复「下载完成但未自动重启新版」问题：为「一键更新」的替换/重启流程新增**独立诊断日志**（写入安装包同目录 `update.log`），
  PowerShell 替换脚本每一关键步骤（等待退出 / 移动新包 / 删除旧包 / 启动新版）都会记录执行结果与错误，出错不再静默，
  便于精确定位被安全软件拦截、中文路径、退出时序等问题
- PowerShell 子进程改为捕获 stdout/stderr 与 spawn 错误，全部写入 `update.log`
- 版本号与 GitHub Release `v1.0.7` 对齐

## [1.0.6] - 2026-08-28

### 修复 / Fixed

- 修复「一键更新」完成后不自动重启的问题：替换/重启逻辑由 .bat + chcp 65001 + start 改为独立的 PowerShell 进程执行（路径经 Base64/UTF-16LE 传入，脚本保持纯 ASCII），彻底规避中文路径与代码页在批处理中偶发失效导致的脚本中断
- 版本号与 GitHub Release `v1.0.6` 对齐

## [1.0.5] - 2026-08-28

### 变更 / Changed

- 修复「检查更新」误报 HTTP 404：当仓库仅存在预发布版本时，GitHub `/releases/latest` 会返回 404，现自动回退到版本列表取最新发布
- 新版本发布但尚未上传安装包时，提示「暂无可下载的安装包」，不再误报网络错误
- 相同内容的提示（Toast）不再重复叠加，避免通知风暴
- 版本号与 GitHub Release `v1.0.5` 对齐

## [1.0.4] - 2026-08-28

### 变更 / Changed

- 修复更新下载失败：直连 GitHub 下载被网络限制时，自动回退到国内可用加速镜像（ghfast.top / gh.ddlc.top / gh-proxy.com），并做下载完整性校验，全部失败时才提示网络错误
- 更新提醒不再弹 Windows 系统托盘气泡/对话框，改为 App 界面内柔和呈现：右上角常驻气泡（可手动关闭）+ 侧栏「关于」与「检查更新」按钮小红点
- 打包产物与 GitHub Release 版本号对齐为 1.0.4

## [1.0.3] - 2026-08-28

### 新增 / Added

- 一键静默更新：发现新版本后点击「立即更新」即可自动下载、替换安装包并重启，无需手动去网页下载
- 更新过程展示进度浮层：下载百分比 / 正在重启 / 失败原因提示
- 更新提醒：启动后 5 秒自动检查，且运行期间每 4 小时周期检查；发现新版以 Windows 托盘气泡提醒，点击气泡即可一键更新
- 「关于」页显示"有新版本"气泡徽标，点击即可一键更新
- 「关于」页「项目主页」指向真实仓库 `mdw-0254/aurora-ladder`

### 变更 / Changed

- 更新检查弹窗按钮由「去下载」改为「立即更新」，更新全程自动化
- 非便携版（开发模式）运行时仍退回打开下载页，保证可用性

## [1.0.2] - 2026-08-27

### 新增 / Added

- 托盘小图标随连接状态同步切换：连接成功后托盘图标变为绿色变体，断开后恢复原色
- 主窗口任务栏图标随连接状态尽力同步切换
- 配置更新仓库 `mdw-0254/aurora-ladder`，启用启动自动检查与「关于」页手动检查更新（基于 GitHub Releases）

### 变更 / Changed

- 优化「已连接」绿色图标配色：翠绿渐变、更鲜亮醒目，深色背景衬托发光质感

### 修复 / Fixed

- 修复旧版打包程序占用单实例锁导致新版无法启动的问题

## [1.0.1] - 2026-08-27

### 新增 / Added

- 暗色 / 浅色主题下节点卡片、正文文字的颜色与对比度优化

### 变更 / Changed

- 界面布局重构：标题栏整合品牌、日期天气与连接状态；总览 Hero 区改为左右布局，雷达动效移至右侧，页面整体更紧凑
- 总览指标（运行 / 延迟 / 流量）布局调整并垂直居中
- 默认窗口尺寸调整为 1022×600，最小尺寸 960×560
- 移除界面与文档中所有 VPN 字眼，品牌统一为 Aurora / 极速

### 修复 / Fixed

- 修复暗色主题下部分文字（节点名称、辅助说明、状态提示）对比度过低的问题

## [1.0.0] - 2026-08-26

### 新增 / Added

- 订阅导入：粘贴订阅链接一键导入节点，自动识别国家 / 地区并归档
- 多协议解析：vmess / vless / ss / ssr / trojan / hysteria2 / tuic / wireguard / Clash YAML
- 批次管理：每次导入独立成批，支持一键删除整批节点，旧节点自动归入「历史导入」
- 一键连接 / 断开，支持系统代理自动开关
- 实时流量监控：上传 / 下载速度、累计流量、今日用量、运行时长、实时速度曲线
- 连接会话管理：实时查看当前活跃的代理连接
- 完整日志系统：记录启动、连接、代理等事件
- 顶部信息栏：自动显示日期、城市天气与连接状态
- 深色 / 浅色双主题：紫青科技风深色 + 央视红金白浅色
- 原生 Windows 托盘：关闭最小化到托盘，后台常驻
- 版本更新：启动后自动检查 + 「关于」页手动检查，基于 GitHub Releases

### 修复 / Fixed

- 修复浅色主题下按钮样式被覆盖导致按钮"消失"的问题
- 修复 Shadowsocks TCP 分包导致的解密 nonce 错位问题
- 修复订阅节点国旗 emoji 孤立代理项导致的解析错误
- 修复侧栏速率显示被截断的问题

### 变更 / Changed

- 移除所有"演示 / 模拟"相关文本，正式发布
- 连接按钮图标改为插头样式，更直观体现"连接"含义

[1.0.0]: https://github.com/mdw-0254/aurora/releases/tag/v1.0.0
